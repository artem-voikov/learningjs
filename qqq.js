// Input : 
// a[] = {1, 2, 3, 4, 5, 10}; 
// b[] = {2, 3, 1, 0, 5}; 

// Output : 4 and 10 are present in first array, but not in second array. 

// Input : a[] = {4, 3, 5, 9, 11}; 
// b[] = {4, 9, 3, 11, 10}; 

// Output : 5


function unique(a,b){

    a = a ?? [];
    b = b ?? [];

    const result = new Map();
    const max = a.length < b.length ? a.length : b.length;
    const low = a.length < b.length ? b.length: a.length ;

    
    for (let index = 0; index < max; index++) {
        const element = max;
        
        for()

    }
    
    return result;
}

